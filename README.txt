COMMANDS



hugo --canonifyURLs server
// delete al file in basepath (but not src folder)
cp -R * ../.. # da src/public a basepath
cp -R static .. #da src/ a basepath

git add *
git commit -m "a msg"

git push -u origin master -f

// A comment
// modify
